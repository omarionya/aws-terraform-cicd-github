name: Terraform Apply Prod

on:
  push:
    branches:
      - main

jobs:
  apply-prod:
    uses: ./.github/workflows/reusable-terraform.yml
    with:
      environment: prod
      action: apply
    secrets:
      inherit
name: Terraform Apply Prod

on:
  push:
    branches:
      - main

jobs:
  apply-prod:
    uses: omarionya/aws-terraform-cicd-github/.github/workflows/reusable-terraform.yml
    with:
      environment: prod
      action: apply
    secrets:
      inherit
name: Terraform Apply Prod

on:
  push:
    branches:
      - main

jobs:
  apply-prod:
    uses: ./reusable-terraform.yml
    with:
      environment: prod
      action: apply
    secrets:
      inherit